Original upstream design built from the pinned source. Build dependencies installed without lifecycle scripts. Root asset and local navigation paths made relative for Plotform previews and static exports. Original framework source retained in vendor/templates/modern-astro-paper.
Forms are not connected to a backend; submission is blocked to prevent sending to upstream demo services.

Nine unavailable remote example images in upstream tutorial articles are replaced by an explicit image-unavailable placeholder. Remaining display images are vendored with original URLs in vendor-remote/SOURCES.json. Missing upstream favicon.ico reference points to included favicon.svg.

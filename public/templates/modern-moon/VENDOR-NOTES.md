Original upstream design built from the pinned source. Build dependencies installed without lifecycle scripts. Root asset and local navigation paths made relative for Plotform previews and static exports. Original framework source retained in vendor/templates/modern-moon.
Forms are not connected to a backend; submission is blocked to prevent sending to upstream demo services.

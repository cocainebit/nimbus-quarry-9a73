# Local adaptations

Official upstream dist HTML, CSS, JavaScript and bundled imagery are retained. CDN scripts, icon assets and Google Fonts are copied locally with license texts.

- External Google Maps iframe replaced with a labeled local placeholder; connect a map provider to enable it.

Original upstream design built from the pinned source. Build dependencies installed without lifecycle scripts. Root asset and local navigation paths made relative for Plotform previews and static exports. Original framework source retained in vendor/templates/modern-astroship.
Forms are not connected to a backend; submission is blocked to prevent sending to upstream demo services.

The GPL-3.0 original framework source, license, package manifest and build instructions are included in upstream-source.zip. One unavailable upstream example image (placekitten.com) uses an explicit unavailable-image placeholder.

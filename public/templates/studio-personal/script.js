const filterButtons=document.querySelectorAll('[data-filter]');filterButtons.forEach(button=>button.addEventListener('click',()=>{const category=button.dataset.filter;filterButtons.forEach(item=>item.setAttribute('aria-pressed',String(item===button)));document.querySelectorAll('[data-category]').forEach(project=>{project.hidden=category!=='all'&&project.dataset.category!==category});const result=document.querySelector('#filter-status');if(result)result.textContent=category==='all'?'Showing all three selected projects':`Showing ${category} projects`}));const menu=document.querySelector('#mobile-menu');document.querySelector('.menu-toggle')?.addEventListener('click',()=>menu.showModal());document.querySelector('.close-menu')?.addEventListener('click',()=>menu.close());menu?.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>menu.close()));

# Working in this repo

Plotform (dir `~/site-studio`): a local Relume/Base44-style website and app builder.
Several Claude instances may share this tree, and a Codex CLI session (ttys007)
built most of it and is paused on a usage limit until 2026-09-22 19:34. Keep this
file short; detail belongs in `README.md` and `docs/`.

## Who is working on what

One line each: the paths you write and what you are mid-way through. Update it
when you start, not when you finish. If a path is listed against someone, do not
write it.

| instance | writes | mid-way through |
|---|---|---|
| _(claude: plotform continuation, 2026-09-17)_ | `src/Template*.tsx`, `src/template-typography.ts`, `src/source-template-html.ts`, `src/NativeTemplateEditor.tsx`, `src/SourceTemplateCatalogue.tsx`, `tests/browser/{template-typography,studio-*,contemporary-collection}.spec.ts`, `public/templates/studio-*`, `vendor/templates/studio-*`, this file | took over from Codex on 2026-09-17: typography spec fixed, two stale specs fixed, studio editions cleaned up (em dashes, GPL notice), three new editions added through subagents, docs updated. Next: Base44-parity gaps in docs/BASE44_GAP_REVIEW.md |
| _(achi-a5, claude: shared account + pay per action)_ | `server/auth.mjs`, `server/platform-billing.mjs`, `server/app.mjs` (AI routes), `server/platform.mjs` (publish, charge status), `server/backend-ai.mjs`, `server/design-ai.mjs`, `server/index.mjs` (wiring), `src/AccountForm.tsx`, `src/PaymentPrompt.tsx`, `src/AccountChip.tsx`, `src/backend-api.ts`, `src/App.tsx` (header chip, generate call), `src/{RefinePanel,BackendPanel,AppDesignAI,BackendAI}.tsx` (paid calls), the shared-account block at the end of `src/style.css`, `src/AppWorkspace.tsx` (overview primary action), `tests/platform-billing.test.mjs`, `tests/browser/platform-account.spec.ts`, `README.md` (payment section), `.env.example`, plus the settings work: `server/{settings,settings-api,providers,app}.mjs`, `src/Settings.tsx`, the settings block at the end of `src/style.css`, `src/App.tsx` (settings view), `tests/settings.test.mjs` | **done, committed e5ea253, plus 244a50d (amount reads USDC) and 82ead8d (overview button created the wrong collection's record, which is why preset-library.spec.ts had never passed).** Pay per action against `~/platform` SPEC v0.2: no balance anywhere, each paid action is its own charge paid on the platform's payment sheet; unpriced SKUs stay free, and nothing is priced yet. The charge routes it calls exist in SPEC v0.2 only, so paying end to end waits on the platform side. **Settings page added 2026-09-17**: pick the model provider (OpenRouter added) and model in the interface, keys stored server side in gitignored `.local/settings.json` and never returned to the browser, services panel shows what this copy is connected to |
| _(claude subagent A: studio-practice)_ | `public/templates/studio-practice/**`, `vendor/templates/studio-practice/**`, `shared/source-templates-studio-practice.json`, `tests/browser/studio-practice.spec.ts`, one import + spread line in `shared/source-templates.mjs` | **done, committed 029a4d8 (Lintel).** |
| _(claude subagent B: studio-retreat)_ | `public/templates/studio-retreat/**`, `vendor/templates/studio-retreat/**`, `shared/source-templates-studio-retreat.json`, `tests/browser/studio-retreat.spec.ts`, one import + spread line in `shared/source-templates.mjs` | **done, committed 0aafb0d (Strand).** |
| _(claude subagent C: studio-launch)_ | `public/templates/studio-launch/**`, `vendor/templates/studio-launch/**`, `shared/source-templates-studio-launch.json`, `tests/browser/studio-launch.spec.ts`, one import + spread line in `shared/source-templates.mjs` | **done, committed 0e5a8ee (Apogee).** |
| _(claude subagent D: studio-gallery)_ | `public/templates/studio-gallery/**`, `vendor/templates/studio-gallery/**`, `shared/source-templates-studio-gallery.json`, `tests/browser/studio-gallery.spec.ts`, one import + spread line in `shared/source-templates.mjs` | **done (Plinth), spec green.** Abstract art-space edition adapted from AstroPaper (MIT, pinned): oversized wordmark, offset twelve column grid, five hand written SVG marks and an SVG floor plan, no photography, programme filtered by on now / upcoming / past with a list or grid switch. |
| _(claude subagent E: studio-signal)_ | `public/templates/studio-signal/**`, `vendor/templates/studio-signal/**`, `shared/source-templates-studio-signal.json`, `tests/browser/studio-signal.spec.ts`, one import + spread line in `shared/source-templates.mjs` | **done (Overtone), 6 of 6 browser tests green.** Abstract dark record label adapted from the Codrops Kinetic Type Page Transition (MIT, pinned), no GSAP, no photography: twelve drawn sleeves, a catalogue filtering by format and year, and a play state that animates a drawn waveform with no audio. |
| _(claude subagent F: studio-press)_ | `public/templates/studio-press/**`, `vendor/templates/studio-press/**`, `shared/source-templates-studio-press.json`, `tests/browser/studio-press.spec.ts`, one import + spread line in `shared/source-templates.mjs` | **done, committed 633b6dd (Quire).** |
| _(claude subagent G: studio-congress)_ | `public/templates/studio-congress/**`, `vendor/templates/studio-congress/**`, `shared/source-templates-studio-congress.json`, `tests/browser/studio-congress.spec.ts`, one import + spread line in `shared/source-templates.mjs` | **done (Plenum), 3 of 3 browser tests green.** Multi day congress adapted from jekyll-theme-conference (MIT, commit 7d7479e4 pinned): six pages, a CSS grid programme of fifteen minute rows that collapses into a chronological list below 860px, a keyboard operable day tab list plus a track filter, six hand written SVGs including a venue plan and an approach drawing in place of the upstream Leaflet map, no photography. Schedule lives in `tools/programme-data.json`, rendered by `tools/build-programme.mjs`. |
| _(claude subagent H: studio-counter)_ | `public/templates/studio-counter/**`, `vendor/templates/studio-counter/**`, `shared/source-templates-studio-counter.json`, `tests/browser/studio-counter.spec.ts`, one import + spread line in `shared/source-templates.mjs`, this row | **done (Carafe), 6 of 6 browser tests green.** Counter restaurant and wine bar adapted from AstroWind (MIT, pinned 14e1a691): six pages, a real menu card with leaders and a price column that holds one right edge from 1440 down to 320 in tabular figures, prices marked illustrative on the page, kitchen and bar hours as separate lists with a closed Monday, a public holiday note and a today marker, booking by mailto beside a form with no action that says nothing is sent. No photography: five hand written SVG drawings, a CSS paper ground, Playfair Display and Source Serif 4 bundled with their OFL files. |
| _(claude subagent I: studio-terminal)_ | `public/templates/studio-terminal/**`, `vendor/templates/studio-terminal/**`, `shared/source-templates-studio-terminal.json`, `tests/browser/studio-terminal.spec.ts`, one import + spread line in `shared/source-templates.mjs`, this row | **done (Bevel), 7 of 7 browser tests green.** Late nineties desktop edition for an invented software product, adapted from 7.css (MIT, pinned 3e93443) after a licence audit ruled out 98.css and XP.css, whose bundled pixel font is a CC BY-SA FontStruction. Window chrome is the layout (navigator and document are real panes), keyboard operable tabbed inspector, Noto Sans bundled because 7.css ships no font, monospace only in the console and the file sample. |
| _(claude subagent J: studio-workshop)_ | `public/templates/studio-workshop/**`, `vendor/templates/studio-workshop/**`, `shared/source-templates-studio-workshop.json`, `tests/browser/studio-workshop.spec.ts`, one import + spread line in `shared/source-templates.mjs`, this row | **done (Fettle), 4 of 4 browser tests green.** Hand drawn workshop and class programme adapted from PaperCSS (ISC, pinned): its irregular border radii and offset shadow rewritten as tokens, its remote Google Fonts import deliberately dropped. Rough.js (MIT) draws the rules, frames, callout, tools, floor plan and chart ONCE at build time into seeded static SVG; the eleven people are Open Peeps (CC0) rendered from react-peeps (MIT) at build time. Bricolage Grotesque (OFL) bundled locally. 6 pages, programme filtered by level and evening together with a live count of sessions and benches free. No photography, no remote requests. |
| _(claude subagent K: cloudcannon collection)_ | `public/templates/cloudcannon-*/**`, `vendor/templates/cloudcannon-*/**`, `shared/source-templates-cloudcannon.json`, `tests/browser/cloudcannon-collection.spec.ts`, one import + spread line in `shared/source-templates.mjs`, a tab plus its context paragraph in `src/SourceTemplateCatalogue.tsx`, a section at the end of `docs/SOURCE-TEMPLATES.md`, this row | **done, 5 of 5 browser tests green.** Imported the CloudCannon MIT line as a whole collection (new "Publisher library" tab): Jetstream and Sendit (Astro) plus Hydra, Urban, Frisco, Justice, Treat, Edition, Cause and Author (Jekyll, built with a local GEM_HOME since only system Ruby 2.6 exists). Aperture rejected, it is a photography grid. **Every bundled photograph is deleted, not carried through**: the Unsplash Licence bars redistributing photos as a collection, so image slots hold placeholder artwork drawn in code at the original pixel sizes. Also stripped Google Fonts, CDN jQuery, Analytics, Disqus, Maps, Donorbox, Mailchimp, Vimeo, YouTube and CodePen; forms blocked; demo note on every page. Note for the continuation session: I added one tab entry and one paragraph to `SourceTemplateCatalogue.tsx`, nothing else in that file. |
| _(claude subagent L: studio-explain)_ | `public/templates/studio-explain/**`, `vendor/templates/studio-explain/**`, `shared/source-templates-studio-explain.json`, `tests/browser/studio-explain.spec.ts`, one import + spread line in `shared/source-templates.mjs`, this row | **done (Threshold), 8 of 8 browser tests green.** Explorable explanation adapted from ncase/trust (CC0, commit 6ec45d73: chaptered scroll, one widget per step, plus two of its CC0 drawings reused as the queue, no audio because its effects are mixed licence) and roadtolarissa (MIT, commit 3e28b8aa: newsprint ground, 750px measure, charts drawn as SVG from the data, hand drawn annotation arrow, scroll driven chart state). Five pages. Two hand written SVG + plain JS figures, no chart library: a load slider over a drawn curve and a 12 slot morning you step through, both keyboard operable, both announcing state, both with a visible summary sentence. Every number lives in `model.js`, which `tools/build-pages.mjs` also reads, so the drawings, readouts and the numbers written into the prose cannot drift. Reduced motion switches off all scroll driving and both figures still work. Fraunces + Newsreader (OFL) bundled. |
| _(claude: relume-style UI revamp, 2026-09-20)_ | `src/style.css`, `src/app-workspace.css`, `src/catalogue.css`, the other `src/*.css` app-chrome sheets, `DESIGN.md`, this row, the `<aside className="sidebar">` block and the signed-out account dialog in `src/App.tsx`, `src/AccountForm.tsx` (markup only, no auth logic), the header and tool rail markup in `src/Editor.tsx`, `src/dialogs.tsx`, `src/TypedPlaceholderTextarea.tsx`, one added click in `tests/browser/{cloud,catalogue,history,preset-library,platform-account}.spec.ts` | **first pass done 2026-09-20.** App chrome rethemed to tokens measured live from relume.ai (see DESIGN.md): every one-off gray and lilac in these sheets now resolves to a custom property in the `:root` block at the top of `src/style.css`, so restyle through the tokens, not with new hex values. Dashboard, catalogue, settings and editor checked by screenshot; 7 layout specs green. **Sidebar rebuilt 2026-09-20** to the measured Relume `/sites` sidebar (switcher, accent Create project, search bound to the project search, pill nav, one line account footer). `cloud.spec.ts` reads "Server saving enabled" from screen-reader text in that footer, keep it. The separate "Account & server projects" nav row was removed on 2026-09-21 at the owner's request; the footer account row now carries that name as its `aria-label`, which is what seven specs click, so keep the label. Since 2026-09-21 that row opens an account menu (`.account-menu`), not the centred window: signed out it offers Sign in and Settings, signed in it offers the save, import and sign out actions, and any error from those opens the old window to show it. Five specs gained one `Sign in` click for this (`cloud`, `catalogue`, `history`, `preset-library`, `platform-account`). **Sign in and sign up card rebuilt 2026-09-21** to Relume's login: `AccountForm` is a 300px `auth-card` (fields named by `aria-label`, so the specs' `getByLabel` still works) and the signed-out account dialog is a full page `auth-panel` with a hairline `auth-frame`. Members of published apps keep the dark submit, owners get the accent. **Dashboard header removed and cards rebuilt 2026-09-21** to the measured `/sites` cards: the shared account chip moved to the sidebar footer, project and catalogue cards are a hairline frame around a sunken well holding a white 12px tile, with a 34px footer row. The project preview scales from its container width (`scale: tan(atan2(100cqw, 1000px))`), so do not add fixed `transform: scale()` overrides to `.mini-site`. **Editor chrome rebuilt 2026-09-21** to the measured Relume site editor: 48px bar, icon tabs in a pill, Preview and Site settings as square icon buttons that keep those accessible names, accent Export, a 46px rail of hairline tiles, 281px inspector with tinted active rows. **Form system applied 2026-09-21** after Relume's site settings page (block at the end of `src/style.css`): Settings, dialogs, inspector and backend panel share 12px labels over filled 32px fields, ground colour section cards, mono chips and a tinted selected state. **New project dialog rebuilt 2026-09-21** as a settings card (`.modal.new-project`: head, two column `np-grid`, footer bar), wording untouched; every single `select` now draws a chevron from a global rule near the end of `src/style.css`. **Custom scrollbars 2026-09-21**: one global `::-webkit-scrollbar` block at the end of `src/style.css` (standard properties only as the Firefox fallback). Do not set `scrollbar-width` or `scrollbar-color` on individual elements: in current Chrome either one makes that element ignore the `::-webkit` rules, which is why three old `scrollbar-width: thin` lines were removed. **Template preview trapped inside the catalogue, fixed 2026-09-21**: the motion block's `rise` and `pop` entrance animations filled forwards, and in Chrome an element with a filling animation on `transform` is the containing block for `position: fixed` descendants, so the preview's overlay covered only `.source-collection` and scrolled with it. They now fill `backwards` only; keep it that way for any container that can hold a fixed dialog. The preview window was restyled in the block at the end of `src/source-templates.css` (CSS only, `SourceTemplateCatalogue.tsx` untouched). The app starter preview in `src/catalogue.css` got the same chrome the same day; both dark green overlays (`#10180ede`, `#16221cb3`) are gone in favour of the `.modal-backdrop` value, and that dialog's button and text rules are scoped to its own header and footer so they stop leaking into the app rendered inside it. **Typed placeholder on the dashboard composer 2026-09-21** (`src/TypedPlaceholderTextarea.tsx`, lines in `composerPrompts` in `src/App.tsx`): an exact copy of the behaviour recorded on Relume's signed-in intake composer, which writes the real `placeholder` attribute: 31ms a character at an even pace, 1800ms rest on the finished line, an instant clear, the next line straight away, no caret, no fade, three dots typed one by one. Three earlier attempts (crossfade, fading letters, caret with backspacing) were rejected by the owner, so do not reintroduce them. Relume signs the session in `.local/relume/profile` out within hours; `.local/relume/intake.mjs` signs in through a visible window and records in the same run. **Browser alert and confirm boxes replaced 2026-09-21**: `src/dialogs.tsx` exports `confirmDialog()` and `alertDialog()` (both return promises) and a `DialogHost` mounted once in `src/main.tsx`. Use them, never `window.confirm`, `alert` or `prompt`; all eight old call sites are converted. **Dashboard hero centred 2026-09-21** after Relume's intake screen (`.dash-hero` block at the end of `src/style.css`): it fills the first screen on purpose so the project list starts below the fold, and it is shared by the projects and templates views. The Relume session now lives in gitignored `.local/relume/profile`. **Dropdown lists restyled 2026-09-21**: selects stay real `select` elements and use the customizable select (`appearance: base-select`, block at the end of `src/style.css`) where the browser has it, so do not replace them with a custom component; `position-try-order: normal` is what makes the list open downward. Do not click anything that generates in the owner's Relume account when inspecting it: "Generate brief" starts an agent chat at once. Not yet looked at by screenshot: the signed-in backend panel tabs, the payment prompt, the native template editor. The signed-in Relume app was not inspected (Cloudflare human check on its login) |
| _(claude: Vercel deployment, 2026-09-21)_ | `api/index.mjs`, `vercel.json`, the pool size line in `server/db.mjs`, this row | **deployed 2026-09-21 to https://tallow-cinder-7b2e.vercel.app from this folder with `vercel --prod`; the API answers and the Neon database is migrated (24 tables). Sign in still fails for people because production turns on email verification and `SMTP_HOST` on Vercel is still 127.0.0.1: waiting on the owner for real SMTP credentials.** Production values live in gitignored `.local/vercel-prod.env`. From the owner's network, Node needs `--network-family-autoselection-attempt-timeout=5000` to reach Neon (its 250ms per address default is shorter than the round trip to us-east-1). The owner deployed to Vercel (project `tallow-cinder-7b2e`, linked in gitignored `.vercel/`), which built only the frontend, so every `/api` call returned 404 and sign in failed. `api/index.mjs` now runs the same Express app as one Vercel function and `vercel.json` sends `/api/*` and `/healthz` to it and everything else to `index.html`. Differences from `server/index.mjs`, all forced by serverless: no job worker (scheduled workflows do not run there), settings file in `/tmp` (not durable, use env vars), pool of 3. Still needed from the owner: a hosted Postgres URL in `DATABASE_URL`, `APP_ORIGIN` set to the https Vercel URL, a fresh `AUTH_SECRET`, real SMTP (production refuses to start without `SMTP_HOST`), and `HOST` removed. The env vars uploaded on 2026-09-21 are the laptop's and point at 127.0.0.1. Unrelated and older: `tests/source-templates.test.mjs` fails on a fresh clone because `.gitignore`'s `dist/` pattern swallowed `public/templates/modern-portfolio/_next/.../next/dist/...` |
| _(codex, paused)_ | everything, historically | stopped mid-typography on 2026-09-17 02:55. If resumed after the limit resets, it auto-continues its goal: read `git log` first |

## Rules

**Running services belong to the Codex session.** Vite :5173 and the API :3001
(`node --watch`) run from this tree, plus Docker Postgres :55432 and Mailpit
:1025/:8025. Playwright reuses :5173. Do not kill or restart them by pattern; kill
by PID only after checking what the PID is.

**If account flows fail with "The server did not respond (500)", check :3001 first.** `node --watch` does not restart a crashed server child until a file changes; `touch server/index.mjs` restarts it without editing anything. Editing `shared/*.mjs` or `server/*.mjs` while browser specs run restarts the API mid-test and fails them.

**`.env` holds real local secrets** (DB password, auth secret, maybe provider keys).
It is gitignored. Never print it or commit it.

**No invented numbers or em dashes in UI copy.** User rules across all projects.

**Commit your own work before going idle.** The repo had zero commits until the
baseline on 2026-09-17; keep it that way no longer.
